# pi runtime dirs (local state — not canonical config)
.pi/

# Historical/archival docs (local planning records — not canonical config).
# WORKFLOW.md and the workflow-hardening plan are intentionally versioned:
# they are canonical references, not scratch notes.
docs/plans/*
!docs/plans/2026-08-23-workflow-hardening.md
docs/solutions/
CONCEPTS.md
HANDOFF.md
